name: phpldapadmin-coulter
services:
  phpldapadmin:
    image: osixia/phpldapadmin:latest
    container_name: phpldapadmin
    restart: unless-stopped
    environment:
      PHPLDAPADMIN_LDAP_HOSTS: samba-ad-dc
      PHPLDAPADMIN_HTTPS: "false"
      TZ: America/New_York
    ports:
      - "8080:80"
    networks:
      backend:
        mac_address: 52:54:00:02:83:02   # Backend VLAN, slot 2
      services:
        mac_address: 52:54:00:02:23:02   # Services VLAN, slot 2

networks:
  backend:
    external: true
  services:
    external: true

x-casaos:
  architectures: [amd64]
  main: phpldapadmin
  category: Identity
  description:
    en_US: phpLDAPadmin (Coulter Edition) for household Samba AD DC management and LDAP clarity.
  icon: ./icon.png
  screenshots:
    - ./screenshot-1.png
  title:
    en_US: phpLDAPadmin (Coulter Edition)
  tagline:
    en_US: Household LDAP tree browser and editor.
  tags:
    - coulter
    - household
    - ldap
    - identity
